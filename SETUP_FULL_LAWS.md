# 📚 Полная база законов РК — пошаговая инструкция

## Что вы получите

После выполнения всех шагов в базе юриста будет **полный текст основных кодексов РК**:
- Трудовой кодекс (~250 статей)
- Гражданский кодекс (~1200 статей)
- Налоговый кодекс (~800 статей)
- Уголовный кодекс (~500 статей)
- КоАП (~920 статей)
- Кодекс о браке и семье (~270 статей)
- + Другие кодексы по вашему желанию

Итого реально **3000-5000 статей**, что в 30-50 раз больше текущей базы. Юрист сможет цитировать конкретные статьи на 90%+ бытовых вопросов граждан РК.

## ⚠️ Реалистичный таймлайн

Из-за лимита Gemini Embedding (бесплатный тариф — 1500 запросов/день) загрузка займёт:
- **~3000 статей = 2 дня**
- **~5000 статей = 3-4 дня**
- **Все 10000 = неделя**

Скрипт сохраняет прогресс, можно запускать частями.

Если торопитесь — оплатите Gemini paid tier (~$5-10 за всю базу), загрузка займёт пару часов.

---

# Шаг 1: Скачать тексты кодексов с adilet.zan.kz

1. Открыть https://adilet.zan.kz
2. Найти нужный кодекс (например, "Трудовой кодекс Республики Казахстан")
3. На странице кодекса справа есть кнопка **"Скачать"** или **"Распечатать"**
4. Выбрать формат **DOC** или **HTML** (если только DOC — потом сохранить как .txt)
5. Открыть скачанный файл, **Сохранить как → .txt** (обычный текст)

**Прямые ссылки на основные кодексы** (могут меняться):
- Трудовой кодекс: https://adilet.zan.kz/rus/docs/K1500000414
- Гражданский кодекс (общая часть): https://adilet.zan.kz/rus/docs/K940001000_
- Гражданский кодекс (особенная часть): https://adilet.zan.kz/rus/docs/K990000409_
- Налоговый кодекс: https://adilet.zan.kz/rus/docs/K1700000120
- Уголовный кодекс: https://adilet.zan.kz/rus/docs/K1400000226
- КоАП: https://adilet.zan.kz/rus/docs/K1400000235
- Кодекс о браке и семье: https://adilet.zan.kz/rus/docs/K1100000518

# Шаг 2: Положить файлы в data/raw-laws/

В вашей папке проекта **qamqor** создайте подпапку `data/raw-laws/` (уже создана если у вас новая версия). Положите туда скачанные .txt файлы с **точно такими именами**:

| Файл (имя как сохранить) | Что это |
|---|---|
| `trudovoy.txt` | Трудовой кодекс |
| `grazhdansky.txt` | Гражданский кодекс (общая + особенная части в одном файле) |
| `nalogovy.txt` | Налоговый кодекс |
| `ugolovny.txt` | Уголовный кодекс |
| `koap.txt` | Кодекс об административных правонарушениях |
| `brak_semya.txt` | Кодекс о браке и семье |
| `zemelny.txt` | Земельный кодекс (опционально) |
| `zdorovie.txt` | Кодекс о здоровье народа (опционально) |
| `predprinim.txt` | Предпринимательский кодекс (опционально) |
| `zhilishnyy.txt` | Закон о жилищных отношениях (опционально) |
| `potrebiteli.txt` | Закон о защите прав потребителей (опционально) |
| `osms.txt` | Закон об ОСМС (опционально) |
| `pensii.txt` | Закон о пенсионном обеспечении (опционально) |

Можете положить только те, что нужны. Скрипт обработает только найденные.

# Шаг 3: Распарсить тексты в JSON

В терминале в папке qamqor:

```bash
node scripts/parse-laws.js
```

**Что произойдёт**: скрипт прочитает все .txt из `data/raw-laws/`, разрежет их на статьи по регексу (ищет `Статья N. Заголовок`), сложит в JSON-файл `data/parsed-laws.json`.

Вывод будет примерно такой:

```
🔍 Парсинг законов РК...

✅ trudovoy.txt              → 247 статей (Трудовой кодекс РК)
✅ grazhdansky.txt           → 1158 статей (Гражданский кодекс РК)
✅ nalogovy.txt              → 786 статей (Налоговый кодекс РК)
⏭  ugolovny.txt — не найден

📊 ИТОГО:
   Обработано файлов:    3
   Всего статей:         2191
   Уникальных:           2188
   Размер JSON:          5234.5 KB

💾 Результат: data/parsed-laws.json
```

# Шаг 4: Загрузить в админку партиями

```bash
node scripts/upload-chunks.js --url https://qamqor-seven.vercel.app --key ВАШ_ADMIN_PASSWORD
```

**Что происходит**:
- Загружается пачками по 30 чанков
- Между пачками — пауза 5 секунд
- Прогресс сохраняется в `data/.upload-progress.json`
- При исчерпании лимита Gemini — останавливается, можно продолжить **на следующий день**

Вывод:
```
📚 Загружено 2191 чанков из parsed-laws.json

📦 Пачка 1/74 (1-30 из 2191)... ✅ +30 (skip:0, fail:0)
📦 Пачка 2/74 (31-60 из 2191)... ✅ +29 (skip:1, fail:0)
...
📦 Пачка 50/74 (1471-1500 из 2191)... ✅ +30 (skip:0, fail:0)
📦 Пачка 51/74 (1501-1530 из 2191)... ❌ +0 (skip:0, fail:30)

⚠️  Похоже на исчерпание лимита Gemini Embedding (1500/день).
   Прогресс сохранён. Запустите скрипт снова завтра.

📊 ИТОГИ:
   Добавлено:   1500
   Пропущено:   1
   Ошибки:      0
```

На следующий день — снова запустить ту же команду, продолжит с того места где остановился.

# Шаг 5: Проверка

После полной загрузки — открыть админку:
```
https://qamqor-seven.vercel.app/api/admin/legal?key=ВАШ_ADMIN_PASSWORD
```

Внизу будет список всех норм, сгруппированных по источникам. Должны быть тысячи.

В Telegram — задать юристу детальный вопрос, например **"что говорит статья 52 Трудового кодекса про прогул?"** — должен ответить с прямой цитатой статьи.

---

# Если что-то пошло не так

### Парсер находит 0 статей в файле

Регекс ищет паттерн `Статья N. Заголовок\n\nСодержание`. Возможные причины:
- В скачанном файле другой формат (например, "Статья N" без точки)
- Текст не разделён переводами строк
- Файл в неверной кодировке (должна быть UTF-8)

**Решение**: откройте файл в текстовом редакторе, посмотрите как разделены статьи. При необходимости отредактируйте регекс в `scripts/parse-laws.js` (строка `ARTICLE_PATTERN`).

### Все пачки падают с ошибкой 500

Скорее всего админ-эндпоинт не выдерживает таймаут на крупных пачках. Уменьшите `BATCH_SIZE` в `scripts/upload-chunks.js` с 30 до 10.

### Хочу пропустить дубликаты которые уже в базе

Скрипт делает это автоматически: `bulkAddChunksToDb` пропускает чанки с уже существующим `id`.

### Хочу удалить всё и загрузить заново

В админ-странице есть кнопка **"Переиндексация"** — она перезапишет всю базу из `legal-data.js` (45 курированных норм). Потом снова загрузить через upload-chunks.

Или вручную: `redis-cli DEL legal:knowledge` через Upstash консоль.

---

# Альтернатива: платный Gemini

Если 1500/день мало:
1. https://aistudio.google.com → Settings → **Upgrade**
2. Привязать карту (не нужно платить вперёд — оплачивается по факту)
3. Цена embedding: **$0.0001 за 1000 символов**
4. На 10000 чанков (~5 МБ текста) = **$0.50**. Реально копейки.

После апгрейда — лимит 1500/мин (а не /день). Загрузите всю базу за полчаса.
